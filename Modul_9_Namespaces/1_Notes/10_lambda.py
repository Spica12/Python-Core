# lambda функція


sum_lambda = lambda x, y: x + y

# Це поганий тон використовувати змінну для присвоєння лямбди функції

numbers = [1, 2, 3, 4, 5]

for i in map(lambda x: x**2, numbers):
    print(i)

# 1
# 4
# 9
# 16
# 25


# У map можна передавати будь-яку функцію, яка приймає один аргумент 
# і будь-яку послідовність, за якою можна ітеруватися (список, кортеж, 
# словник, множини, другий генератор/ітератор).